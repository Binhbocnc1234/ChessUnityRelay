# ChessUnity Relay Server

WebSocket-based room matchmaking and packet relay server for ChessUnity.

## Getting Started

1. **Install dependencies**:
   ```bash
   npm install
   ```

2. **Start the server**:
   ```bash
   npm start
   ```

By default, the server listens on port `8080` (or the port specified by the `PORT` environment variable).
